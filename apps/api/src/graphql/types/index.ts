import User from "./User";

// Aggregator for type-resolvers (field resolvers attached to schema types,
// as opposed to root Query/Mutation resolvers). Add new types here as the
// schema grows.
export default {
  User,
};
