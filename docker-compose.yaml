version: '3.9'
services:
  turbo-temp:
    restart: on-failure
    build: .
    ports:
      - 3000:3000
      - 3001:3001
      - 4000:4000
    depends_on:
      - redis-database

  redis-database:
    image: "redis:alpine"
    ports:
      - "6379:6379"
